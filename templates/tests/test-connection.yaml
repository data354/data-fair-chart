apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datafair.fullname" . }}-test-connection"
  labels:
    {{- include "datafair.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datafair.fullname" . }}:80']
  restartPolicy: Never
